<Window x:Class="WpfApp_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

         
            <Menu Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Height="30">
                <MenuItem Header="Файл">
                    <MenuItem Header="Заполнить" Click="Fill_Click"/>
                    <MenuItem Header="Очистить" Click="Clear_Click"/>
                    <Separator/>
                    <MenuItem Header="Сохранить" Click="Save_Click"/>
                    <MenuItem Header="Загрузить" Click="Load_Click"/>
                    <Separator/>
                    <MenuItem Header="Выход" Click="Exit_Click"/>
                </MenuItem>
                <MenuItem Header="Действия">
                    <MenuItem Header="Найти строку с наибольшей суммой" Click="Calculate_Click"/>
                </MenuItem>
                <MenuItem Header="Справка" Click="About_Click"/>
            </Menu>

 
            <TextBlock Text="Работа с матрицей" FontSize="16" FontWeight="Bold" 
                   Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,25,0,10"/>

       
            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                <TextBlock Text="Строки:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="txtRows" Text="3" Width="40" />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,10">
                <TextBlock Text="Столбцы:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="txtCols" Text="4" Width="40"/>
            </StackPanel>

         
            <DataGrid x:Name="dataGrid" Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,10"
                  AutoGenerateColumns="True" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"/>

          
            <Button Content="Заполнить матрицу" Click="Fill_Click" 
                Grid.Row="3" Grid.ColumnSpan="2" Padding="15,5" Margin="0,0,0,10" 
                FontWeight="Bold" Background="LightBlue"/>

        
            <TextBlock x:Name="txtResult" Grid.Row="4" Grid.ColumnSpan="2" 
                   FontSize="14" FontWeight="Bold" Foreground="Blue" TextWrapping="Wrap" 
                   HorizontalAlignment="Center" TextAlignment="Center"/>
        </Grid>


    
</Window>
